I wish to write high quality OCaml library bindings to the Karakeep instance.
You can assume while testing that an API key to the <https://hoard.recoil.org>
instance is in .karakeep-api.

Examine the source code for Karakeep in karakeep-src/ as a reference.
Maintain helpful API documentation inside the doc/ directory as a set of
Markdown files that reference each other.
